import { defineSchema } from 'convex/server';

// Caddie tables land in Phase E (blueprint step 17): the Convex mirror of the
// core types. Phase B persistence is local-only (src/data) — Convex is additive.
export default defineSchema({});
