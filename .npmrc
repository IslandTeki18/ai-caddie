engine-strict=true
save-exact=false
# Expo/Metro and many RN tools cannot resolve pnpm's symlinked node_modules;
# a hoisted (npm-like) layout is required for the mobile app to bundle.
node-linker=hoisted
strict-peer-dependencies=false
